<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <style>
  body {
    margin: 0;
    padding: 20px;
    background-color: #f5f5f5;
    font-family: Arial, sans-serif;
    color: #333333;
  }
  
  .container {
    width: 600px;
    max-width: 600px;
    margin: 0 auto;
    background: #ffffff;
    padding: 20px;
    border: 1px solid #dddddd;
    border-left: 6px solid #d9534f; /* subtle danger accent */
    border-radius: 8px;
  }
  
  .title {
    color: #c9302c; /* muted red */
    font-size: 20px;
    font-weight: bold;
    margin-bottom: 15px;
  }
  
  .intro {
    font-size: 14px;
    line-height: 1.6;
    margin-bottom: 15px;
    color: #a94442; /* alert-style muted red */
  }
  
  .district-heading {
    font-size: 16px;
    background-color: #f9eaea; /* soft danger tone */
    padding: 8px;
    border-left: 4px solid #d43f3a;
    margin-top: 20px;
    margin-bottom: 10px;
    color: #333333;
  }
  
  table {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 25px;
  }
  
  th {
    background-color: #d9534f; /* bootstrap danger */
    color: #ffffff;
    padding: 8px;
    text-align: left;
    font-size: 13px;
    border: 1px solid #dddddd;
  }
  
  td {
    border: 1px solid #dddddd;
    padding: 8px;
    font-size: 13px;
  }
  
  .even-row {
    background-color: #ffffff;
  }
  
  .odd-row {
    background-color: #fdf2f2; /* hint of red tint */
  }
  
  .footer-text,
  .signature {
    font-size: 14px;
    line-height: 1.6;
    margin-bottom: 15px;
    color: #555555;
  }
  
  a {
    color: #c9302c; /* match alert theme */
    text-decoration: none;
  }
  
  a:hover {
    text-decoration: underline;
  }
  
  .copyright {
    font-size: 12px;
    color: #888888;
    text-align: center;
    margin-top: 20px;
  }
  
  </style>
</head>
<body>
  <div class="container">
    <h1 class="title">
      DDE Sites Synchronization Issues Summary
    </h1>

    <p class="intro">
      <%= @last_synced_data[:total_sites_with_issue] %> site<%= 's' if @last_synced_data[:total_sites_with_issue] != 1 %> have connectivity issues
      Below is the list of districts and their respective sites that have been offline for more than <strong>3 days</strong>.
    </p>

    <% @last_synced_data[:districts].each do |district| %>
        <h2 class="district-heading">
        <%= district[:name] %> (<%= district[:sites_last_activity_greater_than_3_days_sites] %> out of <%= district[:total_sites] %> site<%= 's' if district[:total_sites] != 1 %> )
         </h2>
    <% end %>

      <p class="footer-text">
      For the full interactive report, <a href="<%= @report_url %>">click here to view it in your browser</a>. For real-time status, visit:
      <a href="https://ddedashboard.hismalawi.org" target="_blank">DDE Dashboard</a>.
    </p>


    <p class="signature">
      Best regards,<br><strong>Demographics Data Exchange</strong>
    </p>

    <div class="copyright">
      &copy; <%= Time.now.year %> Demographics Data Exchange. All rights reserved.
    </div>
  </div>
</body>
</html>
