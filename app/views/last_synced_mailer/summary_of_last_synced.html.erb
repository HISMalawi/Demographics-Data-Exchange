<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <style>
  body {
    margin: 0;
    padding: 20px;
    background-color: #f5f5f5;
    font-family: Arial, sans-serif;
    color: #333333;
  }
  
  .container {
    width: 600px;
    max-width: 600px;
    margin: 0 auto;
    background: #ffffff;
    padding: 20px;
    border: 1px solid #dddddd;
    border-left: 6px solid #d9534f; /* subtle danger accent */
    border-radius: 8px;
  }
  
  .title {
    color: #c9302c; /* muted red */
    font-size: 20px;
    font-weight: bold;
    margin-bottom: 15px;
  }
  
  .intro {
    font-size: 14px;
    line-height: 1.6;
    margin-bottom: 15px;
    color: #a94442; /* alert-style muted red */
  }
  
  .district-heading {
    font-size: 16px;
    background-color: #f9eaea; /* soft danger tone */
    padding: 8px;
    border-left: 4px solid #d43f3a;
    margin-top: 20px;
    margin-bottom: 10px;
    color: #333333;
  }

   .region-heading {
    font-size: 16px;
    background-color: #8B0000; /* soft danger tone */
    padding: 8px;
    border-left: 4px solid #d43f3a;
    margin-top: 20px;
    margin-bottom: 10px;
    color: #333333;
  }


  
  table {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 25px;
  }
  
  th {
    background-color: #d9534f; /* bootstrap danger */
    color: #ffffff;
    padding: 8px;
    text-align: left;
    font-size: 13px;
    border: 1px solid #dddddd;
  }
  
  td {
    border: 1px solid #dddddd;
    padding: 8px;
    font-size: 13px;
  }
  
  .even-row {
    background-color: #ffffff;
  }
  
  .odd-row {
    background-color: #fdf2f2; /* hint of red tint */
  }
  
  .footer-text,
  .signature {
    font-size: 14px;
    line-height: 1.6;
    margin-bottom: 15px;
    color: #555555;
  }
  
  a {
    color: #c9302c; /* match alert theme */
    text-decoration: none;
  }
  
  a:hover {
    text-decoration: underline;
  }
  
  .copyright {
    font-size: 12px;
    color: #888888;
    text-align: center;
    margin-top: 20px;
  }
  
  </style>
</head>
<body>
  <div class="container">
    <h1 class="title">
      DDE Sites Synchronization Issues Summary
    </h1>

    <p class="intro">
      <strong><%= @last_synced_data[:total_sites_with_issue] %> site<%= 's' if @last_synced_data[:total_sites_with_issue] != 1 %></strong> out of
      <strong><%= @last_synced_data[:total_sites] %> site<%= 's' if @last_synced_data[:total_sites] != 1 %></strong> have synchronization issues
    </p>

    <% (@last_synced_data[:regions] || []).each do |region| %>
       <% 
        total_region_sites = region[:total_sites]
        total_region_sites_with_issues = region[:total_sites_with_issue]
      %>

      <h2 class="district-heading region-heading" style="background-color: #c9302c; color: #fff; font-size: 20px; border-left: 4px solid #d43f3a;">
        <%= region[:name] %> Region (
          <%= total_region_sites_with_issues %> out of <%= total_region_sites %> site<%= 's' if total_region_sites != 1 %> 
        )
      </h2>
      <% (region[:districts] || []).each do |district| %>
        <% count = district[:sites_last_activity_greater_than_3_days_sites] || 0 %>
        <% total = district[:total_sites] || 0 %>
        <% if count > 0 && total > 0 %>
          <h2 class="district-heading" style="display: flex; justify-content: space-between; align-items: center;">
            <span>
              <%= district[:name] %> (
                <%= count %> out of <%= total %> site<%= total != 1 ? 's' : '' %>)
            </span>
            <span style="display: flex; align-items: center; gap: 8px;">
              <% if district[:change] && district[:change_percent] && district[:change] != 0 %>
                <span style="font-weight:bold; color:<%= district[:change] < 0 ? 'green' : 'red' %>; min-width: 90px; text-align: right; display: inline-block; white-space: nowrap;">
                  <%= district[:change] > 0 ? '▲' : '▼' %>
                  <%= district[:change].abs %> (<%= district[:change_percent].abs %>% )
                </span>
              <% end %>
            </span>
          </h2>
        <% end %>
      <% end %>
    <% end %>

     <p class="footer-text">
      For the full interactive report, <a href="<%= @report_url %>">click here to view it in your browser</a>. 
      For real-time status, visit: <a href="https://ddedashboard.hismalawi.org" target="_blank">DDE Dashboard</a>.
      <br>
      <strong>Note:</strong> Access to the above links requires connection to the <em>MOH VPN</em>.
    </p>

    <p class="signature">
      Best regards,<br><strong>Demographics Data Exchange</strong>
    </p>

    <div class="copyright">
      &copy; <%= Time.now.year %> Demographics Data Exchange. All rights reserved.
    </div>
  </div>
</body>
</html>

